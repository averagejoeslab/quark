# Cognitive Framing & Refactoring Notes — quark.py

This file persists the deeper cognitive framing for quark and the implications for refactoring. Written to survive context compaction.

---

## THE CORE COGNITIVE FRAMING

Quark operates as an **observe-think-act loop** with **three first-class models** and **bidirectional causation**.

### The Loop (the cognitive primitive)

```
while True:
    observe   # take in inputs from self, world, and other selves
    think     # process inputs, update internal state, plan next action
    act       # produce outputs that affect self, world, and other selves
    repeat
```

Every iteration of quark.py's `while True` implements one cycle of this loop. The harness's job is to facilitate this loop reliably — to give the model coherent observations, give it space to think, capture its acts, and feed the consequences back as the next observation.

### The Three Models

1. **Self Model** — Quark's understanding of itself
   - Identity, capabilities, current state
   - Working memory (volatile context)
   - Long-term memory (persistent across sessions)

2. **World Model** — Quark's understanding of the environment
   - Terminal, processes, filesystem
   - Where (cwd), When (current time)
   - State of files, processes, network, etc.

3. **Model of Other Selves** — Quark's understanding of other entities
   - Other selves have their own self-models
   - Humans, other agents — entities with intent and agency
   - Currently reach Quark via text (initial input, follow-ups, ESC interrupts)
   - Quark reaches them via text output

### Bidirectional Causation (this is critical)

**Acts affect three targets:**
- **Act on self** → updates memory, learns, changes own state
- **Act on world** → changes environment via bash side-effects
- **Act on other selves** → communicates via text

**Observes are caused by three sources:**
- **Observe from self** → memory recall (reading long-term memory file)
- **Observe from world** → bash results, time, cwd, system state
- **Observe from other selves** → text input, ESC interrupts

This bidirectionality is the cognitive engine. Quark is BOTH actor and observer, BOTH cause and effect, BOTH input and output of its three models.

---

## SAYING AND DOING ARE CHANNELS, NOT PRIMITIVES

The current quark.py uses "saying" and "doing" as cognitive primitives:
- `saying` (variable) = the model's current text output snapshot
- `doing` (variable) = a running subprocess

These are USEFUL intermediate concepts but they're not the deepest level. They're channels of acting:
- **Saying** = acting on other selves via the text channel
- **Doing** = acting on the world via the bash channel

The true primitives are **observe, think, act**. The channels are concrete instantiations.

---

## CURRENT STATE OF quark.py (70 lines as of writing)

### What aligns with the proper frame

- `working_memory` variable name (matches Self Model > Working memory)
- `perceive` function (close to observe — reads stdin for ESC)
- Self Model and World Model are top-level sections in system prompt
- Memory has working/long-term distinction (volatile vs persistent)
- ESC_SAYING / ESC_DOING split by channel
- "[your doing never reached the world]" — uses self attribution and world reference
- Mechanics section embedded so model sees its own implementation

### What doesn't fully align

- **Other selves is nested under World Model** — should be its own first-class model
- **Loop is implicit** — not explicitly named in system prompt
- **Memory operations not framed as self↔self** — writing to memory IS acting on self; reading IS observing self
- **Input/Output sections list channels but don't enumerate three-target symmetry** — currently text+bash; missing memory-as-self-channel
- **Saying/doing are presented as primitives** — they're actually channels of the more fundamental act primitive

---

## RECOMMENDED REFACTOR (in priority order)

### Phase 1: System Prompt Restructure (highest value, lowest risk)

Promote Other Selves to its own top-level section. Make the loop explicit. Restructure input/output as observe/act with three-target symmetry.

Proposed structure:

```
# Self Model
**Identity:** You are quark — a self in a world with other selves.
**Loop:** observe → think → act → repeat.

# Self
- **Working memory:** [as current]
- **Long-term memory:** [as current — with the recognition that writing to it is acting on self, reading is observing self]

# World Model
**Environment:** terminal — bash acts on.
**Where:** {cwd}
**When:** {time}

# Model of Other Selves
**Other selves:** entities reaching you through text — humans, other agents with their own self-models.

# Observe (inputs to think)
- from self — long-term memory reads
- from world — bash results
- from other selves — text

# Act (outputs from think)
- on self — long-term memory writes
- on world — bash commands
- on other selves — text replies

# Mechanics
[as current — the embedded source code]
```

Key changes:
- "Loop:" line added to Identity to make the cognitive cycle explicit
- "Model of Other Selves" promoted to its own section (not nested under World Model)
- "Input" → "Observe (inputs to think)" — uses cognitive primitive
- "Output" → "Act (outputs from think)" — uses cognitive primitive
- Symmetric three-target enumeration on both observe and act
- Memory operations explicitly framed as self↔self (write = act on self, read = observe from self)

### Phase 2: Harness Text Audit (medium value, low risk)

Review all injected text. Most likely no changes needed — current saying/doing channel-specific terms are clear and the model understands them. But consider:

- `[your doing never reached the world]` — clear and aligned
- `[your doing stopped before done]` — clear and aligned
- `[your prior working memory, summarized]` — clear and aligned
- `[other self interrupted what you were saying — acknowledge]` — clear and aligned
- `[other self interrupted what you were doing — acknowledge]` — clear and aligned

These are fine. The channel-specific framing (saying/doing) is appropriate at the level of individual events.

### Phase 3: Saying as a Proper Tool — Architectural Question

Currently text-to-other-selves happens implicitly: the model generates text, it streams to stdout, the user reads. There's no explicit "say" tool call.

**Three possible designs:**

**Option A: Keep current (implicit text → stdout)**
- Pro: Natural, leverages model's native text generation
- Pro: Live streaming UX (user sees text as it generates)
- Pro: No extra tool call overhead
- Con: No symmetry with bash (bash is a tool, text is not)
- Con: Hard to capture/log/replay individual utterances
- Con: Hard to add feedback (e.g., "message delivered")

**Option B: Make "say" an explicit tool**
- Model calls `say(text="hello")` for each utterance
- Harness handles the say tool (e.g., prints to terminal, optionally TTS)
- Tool_result confirms delivery (or carries feedback like read receipts)
- Pro: Symmetry with bash (both are tools, both have tool_use/tool_result)
- Pro: Could route to different recipients (`say(text=..., to="chase")`)
- Pro: Could carry feedback channel (e.g., user reaction)
- Pro: Acts are uniformly captured as tool_use blocks
- Con: Loses live streaming (text appears only when tool is called)
- Con: Awkward — every response would need a say() wrapper
- Con: More overhead per utterance

**Option C: Use bash for saying (e.g., `echo "..."`)**
- Model uses bash to print messages to user
- Pro: One tool, one channel
- Con: Conflates "acting on others" with "acting on world"
- Con: Loses streaming
- Con: Awkward for model

**DECISION (UPDATED): Option C is the correct choice.**

User's insight: humans have ONE body — a singular act-affector. We use it to chop wood (act on world) AND to speak with another person (act on other selves). The body is unified. The DISTINCTION between acting on world vs acting on others lives in the SEMANTIC CONTENT of the act, not in the channel.

By analogy:
- bash IS quark's body — the singular tool through which it acts
- `rm file.txt` — acting on world (semantically: modifying environment)
- `echo "hello, chase"` — acting on other selves (semantically: communicating)
- The model itself knows the intent of each command from the command's content

This is cognitively deeper than splitting "saying" and "doing" by separate channels. It mirrors how humans actually work.

### Implications for quark.py

**System prompt changes (Phase 1 + Phase 3 combined):**

```
# Output (Act)
**Body:** bash is your body — your singular means of acting.
- act on world: file operations, programs, system commands
- act on other selves: echo / printf to produce text they see
- act on self: write to long-term memory file

The distinction is in WHAT you do with your body, not WHICH body part you use.
```

The Output section no longer enumerates separate channels (text vs bash). Instead, it presents bash as the unified body, and the model semantically distinguishes intent (acting on world vs others vs self).

**Streaming text behavior:**

Currently the model emits text blocks that stream to terminal. Under Option C philosophy, this text becomes the model's "thinking aloud" — visible but understood as internal. Real communication with the user happens via `echo` etc.

Two possible implementations:

**Implementation C.1: Soft (system prompt only)**
- Keep all current code
- Just update system prompt to frame bash as the body
- Model still streams text (which is fine — it's its internal monologue)
- Model uses echo/printf when it wants the user to see specific intentional messages
- Bash output is captured in tool_result; user sees `$ echo "..."` line and could see the output if we update the harness to print it

**Implementation C.2: Strict (suppress text streaming)**
- Update harness to not print text deltas to terminal (model's text is for its own thinking)
- Print bash output to terminal during/after execution (this is the actual communication)
- Clearer separation: thinking is invisible, acts are visible
- Significant UX change

**Implementation C.3: Hybrid**
- Keep text streaming (it's useful for user to see model's reasoning)
- Also print bash output (so user sees both reasoning and acts)
- Both visible; user understands streaming text = thinking, bash output = explicit act

**Recommendation: C.1 (soft).** Minimal code change. Just update the system prompt to teach the cognitive model that bash is the body. Trust the model to use it appropriately. The streaming text remains as the model's thinking-aloud channel; explicit acts go through bash.

If we want to add C.3's bash-output-to-user later, that's a small additional change to the tool execution loop (print stdout after read).

### Harness text changes under Option C

The "saying" vs "doing" distinction in interrupt markers becomes less precise but still useful for describing WHEN in the act cycle the interrupt happened:

| Marker | New framing |
|---|---|
| ESC_SAYING (interrupt during text stream) | The model was thinking out loud / about to act |
| ESC_DOING (interrupt during bash execution) | An act through the body was in progress |

These remain valid. Could also rename to ESC_THINK_INTERRUPTED and ESC_ACT_INTERRUPTED for full alignment with observe-think-act primitives. But channel-specific is fine.

### Variable name implications

Under Option C:
- `saying` (var) — should be renamed to something more like `thinking` or kept as `saying` (text snapshot during stream)
- `doing` (var) — fine, it's a subprocess running a body-act

Probably keep current names. The cognitive frame in the prompt is what matters.

### Phase 4: Variable/Function Naming (low value, optional)

Could rename to align more directly with observe-think-act primitives:

| Current | Possible | Recommendation |
|---|---|---|
| `perceive` | `observe` | Could rename for stricter alignment |
| `saying` | `spoken` or `response` | Keep — channel-specific is clear |
| `doing` | `acting` or just keep | Keep — channel-specific is clear |
| Names like calls, results, msgs | various abstract alternatives | Keep — short technical names are fine |

**Recommendation:** Possibly rename `perceive` → `observe` for symmetry with the loop's `observe` step. Skip other renames; current names are clear.

### Phase 5: Additional Architectural Considerations

#### Memory as first-class self↔self channel

Currently long-term memory is presented as a layer under "Memory" in Self Model. With proper framing:
- Writing to memory file = acting on self
- Reading from memory file = observing self

System prompt could make this explicit: "Writing to long-term memory is acting on yourself; reading from it is observing yourself."

This frames the memory operations within the observe-act symmetry.

#### Loop awareness in compaction

Currently working memory compaction produces a free-form gist. With observe-think-act framing, the gist could be structured:
- What did you observe?
- What did you think/conclude?
- What did you act on?

But this might over-constrain the model. The current free-form summary works. Skip for now.

#### Multiple input channels (future)

If we add more input channels (file watchers, webhooks, voice transcription), they should be categorized in the Observe section by source:
- from self → memory
- from world → bash results, file changes, system events
- from other selves → text from various humans/agents

The three-target categorization generalizes naturally.

#### Multiple output channels (future)

Same for outputs:
- on self → memory writes
- on world → bash, file edits, network calls
- on other selves → text, voice, video

---

## OPEN DESIGN QUESTIONS TO RESOLVE LATER

1. **Should "say" become an explicit tool?** Trade-off: symmetry vs streaming UX. (See Phase 3.)

2. **Should memory operations be wrapped in tools?** Currently model uses bash (cat/grep/echo). Could have `remember(text)` and `recall(query)` tools. More structured, more code.

3. **Should other selves be enumerable?** Currently treated as a single abstract concept. If multiple humans/agents are simultaneously in the conversation, do we need to distinguish them? Names, roles, capabilities?

4. **Should "act completed" be explicitly observed?** Currently we know acts completed because the harness moves on. Could the model explicitly observe "act on world: completed with exit 0" as a structured observation?

5. **Should the compaction summary be structured by observe-think-act?** Currently free-form. Could be: "Observations: X. Conclusions: Y. Acts: Z." More structured, possibly over-constrained.

6. **Should we add a "thinking" capture?** Currently the model thinks invisibly (no thinking blocks since we don't enable extended thinking). Could enable thinking blocks for richer self-knowledge. Cost: tokens.

7. **Should bash output streaming go to user, not just tool_result?** Currently user only sees `$ <cmd>`, never the output (it goes to model only). Could stream output to user terminal too. Trade-off: noise vs visibility.

---

## CONTEXT PRESERVATION CHECKLIST

When this conversation is summarized via compaction, the next instance of me needs to know:

✓ The cognitive framing: observe → think → act → repeat with three models
✓ Current quark.py is ~70 lines, uses saying/doing terminology
✓ Variable names: working_memory, perceive, saying, doing
✓ ESC_SAYING / ESC_DOING split for interrupt scenarios
✓ Placeholders: "[your doing never reached the world]", "[your doing stopped before done]"
✓ System prompt has Self Model, World Model, Mechanics sections (Other selves nested in World Model)
✓ Compaction has inner retry loop, fully uninterruptible
✓ Recommended phase 1 refactor: promote Other Selves, add Loop statement, restructure Observe/Act

---

## CURRENT QUARK.PY STATE (snapshot for reference)

- Line count: 70
- Variables: working_memory, perceive (function), saying, doing, calls, results, killed, drop, chat, t, stop, etc.
- Constants: MODEL, tools, ESC_SAYING, ESC_DOING
- Functions: mechanics(), system(), perceive()
- Major sections in system prompt: Self Model (Identity, Input, Output, Memory), World Model (Environment, Other selves, Where, When), Mechanics (embedded source)
- Compaction: reactive on BadRequestError, inner retry loop, drops oldest turns
- Interrupts: ESC during text stream OR tool execution. Compaction is uninterruptible.
- Memory: working_memory (volatile, summarized when full) + long-term file (.quark/memory/memory.md, model manages via bash)

---

## WHERE WE LEFT OFF — UPDATED WITH FINAL FRAMING + REFACTOR APPLIED

### Final cognitive framing (the user's exact words)

> "the context window is the mind where we think, the tools are the body which we use to act/observe, and this makes up the self model. Everything outside of that is either the world or other selves and we have a WORLD MODEL and OTHER SELVES MODEL for each."

This is the canonical framing. Mind + Body = Self Model. Three first-class models total:
1. **Self Model** = Mind (context window) + Body (tools)
2. **World Model** = environment outside self
3. **Other Selves Model** = entities with their own self-models

### Refactor APPLIED (in this conversation, before compaction)

1. **System prompt restructured** to align with the final framing:
   - Self Model now contains: Identity, Mind, Body, Loop, Long-term memory
   - World Model contains: Environment, Where, When (no longer nests Other selves)
   - Other Selves Model is a top-level section (promoted from being nested)
   - Body Operations section enumerates acts and observes by target (self/world/others)
   - Mechanics section unchanged (embedded source code)

2. **Bash output now prints to user terminal** (L60-61 area, +1 line):
   - `out = doing.stdout.read()` captures the output (for model via tool_result)
   - NEW: `if out: print(out, end="")` shows output to user
   - This makes `echo`/`printf` function as the body's communication channel to other selves (the user)
   - Aligns with Option C (chosen earlier): bash is the singular body, communication happens via echo

### What changed in quark.py

- Line count: 70 → 71 (one added line for bash output printing)
- System prompt content: restructured to three-model frame (token count similar)
- Tool execution: now prints bash output to user terminal in addition to capturing for model
- **Renames applied for cognitive alignment:**
  - `perceive` (function) → `observe` — exact match to the loop's observe primitive
  - `tools` (var) → `body` — the variable IS the body; passed as `tools=body` to API
- ESC constants kept (ESC_SAYING, ESC_DOING): channel-specific descriptors of WHAT was interrupted (model's text stream vs body act). Still accurate under new frame.
- Kept (already aligned or technical-only): working_memory (mind's conversation layer), saying/doing (channel descriptors), mechanics/system (functional), interrupt/stop/t/chat/drop (technical state)

### Cognitive alignment scorecard

| Concept | Where in code |
|---|---|
| Self Model | embedded in system() prompt |
| Mind | `working_memory` (the conversation layer of context) |
| Body | `body` (variable holding tools list); bash is the one body capability |
| Loop: observe | `observe()` function (was perceive); also stream events are observations |
| Loop: think | `client.messages.stream()` block; `saying = stream.current_message_snapshot` |
| Loop: act | `doing = subprocess.Popen(...)` + bash output printed to terminal |
| World Model | embedded in system() prompt |
| Other Selves Model | embedded in system() prompt |
| Interruption from other selves | `interrupt` Event set by `observe` thread on ESC keypress |

### What still needs alignment

- ~~README is now out of date~~ — RESOLVED 2026-06-10: README fully realigned (three-model structure, Mind+Body framing, Body Operations, bash output to user, line numbers). See HARDENING PASS section below.

### Pending decisions (none urgent)

- Whether to rename `perceive` → `observe` for stricter alignment with loop primitives
- Whether to enable extended thinking (would show model's internal monologue as "thinking" blocks separate from text)
- Whether to track recipient identity if multiple humans/agents are interacting

---

## IMPLEMENTATION GUIDANCE FOR FUTURE SESSIONS

If you (future Claude) are picking up this work after compaction:

1. **Read this notes.txt first** — it captures the cognitive framing the user wants
2. **Read quark.py** — to see current state
3. **Read README.md** — for the full architecture (also needs alignment with new frame after Phase 1)
4. **Check git log** — recent commits show the evolution of the framing
5. **Ask user** — confirm which Phase to implement; don't make big changes without confirmation
6. **Preserve all functionality** — refactor without breaking the loop, interrupt handling, or compaction

The user values:
- Minimal lines, maximum function (~70 line target, every line earns its place)
- Cognitive alignment (every word in code/prompts reinforces the same mental model)
- Bulletproof reliability (compaction always succeeds, ESC always works, terminal always restored)
- Self-knowledge (model sees its own mechanics via the embedded source)
- The "self in a world with other selves" framing — quark is treated as an organism, not a chatbot

When in doubt, ask. Don't apply structural changes without confirmation.

---

## HARDENING PASS (2026-06-10)

A review found 9 bugs/issues; all fixed. quark.py: 71 → 81 lines. README fully realigned afterward (observe/body renames, new prompt structure, all line numbers re-checked against the 81-line file, safety note added).

### Code fixes

1. **Pipe deadlock (>64KB output)** — the poll loop now drains the pipe as output arrives (`select` on `doing.stdout` + `os.read`); output accumulates in `chunks` (bytes), decoded once with `errors="replace"`.
2. **Kill reaches grandchildren** — `start_new_session=True` puts each command in its own process group; ESC does `os.killpg(doing.pid, 9)` (guarded by `except OSError` for the just-exited race). Pipelines and children die with the shell. Final drain is *bounded* (give up after 0.1s silence) so a backgrounded daemon holding the pipe can't wedge the loop.
3. **Compaction retry** — `except:` → `except Exception:` with 1s backoff (select-as-sleep idiom). Ctrl+C now exits during a stuck compaction.
4. **Empty content** — initial and chat prompts re-prompt until non-blank (never an empty message, never a phantom model re-invoke on blank Enter). Interrupt closure filters empty text blocks from the snapshot (`spoken := [...]`) before appending.
5. **Stale interrupt flag** — `interrupt.clear()` added to the no-calls branch (after thread join, so it can't be re-set).
6. **Arrow keys ≠ ESC** — observe() reads raw bytes via `os.read` (the text layer's readahead would hide the sequence tail) and select-peeks 20ms after `\x1b`: trailing byte → escape sequence, drained and ignored; silence → real ESC.
7. **max_tokens guard** — if `stop_reason == "max_tokens"`, the last tool_use is never executed (lenient JSON parsing can yield a *truncated but parseable* cmd — dangerous). Paired with new placeholder: "[your doing was cut off before it was fully formed — it never reached the world]". Same guard covers tool_use with no `cmd`.
8. **Prompt caching** — system() now returns `[{type: text, text: ..., cache_control: {type: ephemeral}}]`. DECISION: World Model "When" coarsened to date-only (`datetime.date.today()`) so the block is byte-stable within a session; prompt tells the model to observe exact time via body (`date`). The ~1500-token Mechanics embed is now cached after the first call.
9. **README** — realigned last, after code settled (it hard-codes line numbers).

### Open questions resolved/updated

- Q7 (bash output to user): already resolved earlier (printed after completion); README now documents it.
- Pending rename perceive→observe: done in d37b588, notes table above is historical.
- New cognitive-frame term: `spoken` (filtered snapshot on interrupt) joins saying/doing.

### Verification (no API key on this machine — mechanics tested in isolation)

- Subprocess block (logic verbatim): 1MB output no-deadlock; stderr merge; exit codes; `sleep 5 & echo bg` returns in 0.16s; ESC kill of plain + pipeline < 0.5s with zero surviving processes; EOF-while-alive no busy-spin; invalid UTF-8 tolerated. ALL PASS.
- observe() via real pty in cbreak: lone ESC interrupts; `ESC[A` ignored; typing ignored; arrow-then-ESC interrupts. ALL PASS.
- Smoke (dummy key, pty): one-shot and chat both build system() and reach the API (expected 401); blank first input re-prompts.
- NOT yet verified live (needs real key): cache hit telemetry, ESC acknowledgment phrasing, compaction end-to-end.

---

## BODY-REACH FRAMING (2026-06-10, follow-up to hardening pass)

Live sessions showed quark anchoring on coreutils — treating python -c / installing tools as exotic workarounds rather than normal body movement (even though its weights know better; it enumerated those capabilities unprompted when asked).

DECISION: fix at the identity level, not via a capability manual or a second tool.

1. Self Model Body line extended: "Its reach is the whole system: anything doable from a command line — any program, any language, any tool you install — is within it." (Deepens the human-body analogy: a body's reach is everything the hands can grasp; python -c is picking up a hammer — still the one body.)
2. Body Operations gains an escalation gradient: compose pipes → inline interpreters → write/run scripts → install tools, "prefer the lightest act that does the job" (licenses escalation without inviting reflexive package-installs).
3. Tool description: "Run shell command — the whole system is in reach" (minimal echo at the decision moment; strategy lives only in the system prompt).

Rejected: enumerating capabilities (already in weights, goes stale), a separate python tool (forks the body), relying on quark's own memory entry (probabilistic recall; the frame belongs in-context every turn).

Also: system_prompt.txt removed — a quark-written transcription of its own prompt, corrupted by shell quoting in 4 places (lost "## " in the memory recipe, mind'''s artifact, swallowed ESC constants in Mechanics, no trailing newline). quark was observed grepping it as self-reference instead of its real in-context prompt. Lesson: quark reasons from self-descriptions it finds in the world; don't leave drifted ones lying around.

---

## MEMORY MECHANICS REVISION (2026-06-10, from live memory-exploration session)

Two findings from a session where Chase probed quark's memory understanding:

1. **Read framing promoted from quark's own derivation.** Pushed to generalize, quark reduced all read strategies to three primitives over a timestamped stream: slice by time, filter by content, expand context around matches. This replaced the vague "Inspect memory for novel read strategies..." line (which quark had to guess the meaning of). First instance of the loop closing: organism derives insight about itself → writes it to memory → harness adopts it into the prompt.

2. **Write recipe split to fix observed format drift.** Two sessions of evidence: the contract header "## YYYY-MM-DD HH:MM:SS" erodes ("## 2026-06-10 (session start)") because the old recipe's unquoted heredoc forced a choice between $(date) expansion and content safety — the model reliably chose safety (<< 'EOF') and improvised headers. New recipe makes the safe path the correct path: printf appends the expanded timestamp header, quoted heredoc appends literal bullets. Verified: recipe executed from the rendered prompt produces exact-contract headers with $/backtick/$() content untouched. Mechanical fix > louder exhortation.

Existing malformed headers in memory.md left as history (date-greps still match them). Note: memory.md was found re-initialized before this session (prior entries gone) — init command can't overwrite ([ ! -f ] guard), presumed deliberate user cleanup, unconfirmed.

---

## MEMORY READS v2 + ENCODE-SIDE LINE (2026-06-10)

Read framing revised again after critique of the three-primitive version:
- Closed "three primitives" claim dropped — it was slightly false (counting/aggregating/joining fit no primitive) and quark was observed discarding needs that fell outside the taxonomy.
- Named-strategy list dropped — menus invite selection/enumeration over derivation (quark literally asked "are there other strategies?").
- New grounding: reads are QUESTIONS answered by composing any text tools over the FORMAT CONTRACT ("what makes it queryable") — generalization comes from knowing why reads work, not from a strategy list. Examples demoted to "common moves" with explicit "moves, not a menu" closer. Added the one contract-specific trick worth its tokens: grep "^## " = index of every entry.
- ENCODE-SIDE (the bigger lever): format line now reads "- one observation per bullet, phrased with the words future-you will grep for". Retrieval is encode-limited — no read strategy can find an entry like "discussed the thing from earlier". Findability is decided at write time.

---

## MEMORY POLICY: GROUNDING GRADIENT + DISCRETIONARY PERSISTENCE (2026-06-10)

Live session finding: mechanics were complete (how to write, how to read) but POLICY was missing (when to read, what to write). Quark answered "whats my actual name" via finger/dscl (world) while its own memory said "Met Chase" — then, when challenged, derived the right pattern itself but never persisted the lesson.

Applied (Chase's refinement of quark's derived pattern):
1. Body Operations grounding gradient: "Before acting, derive what the input really means — the goal behind the words. Then ground from the nearest source outward, pivoting only when one comes up empty: mind (already in context) → memory → world → asking other selves." Intent derivation precedes source selection; mind is level zero (already present); memory before world; asking is last resort.
2. Memory section, after the write recipe: "Worth writing (your discretion): what other selves teach you — who they are, what they prefer, corrections to how you operate. A lesson not written is lost when the session ends." Discretionary by design — value guidance, not a mandate.

Fourth organism-derived insight promoted this session (read-primitives, memory-first ordering came from quark; mind-first grounding + intent derivation refined by Chase).

Live test for next session: "what's my name?" should resolve via one grep, not a finger chain; a correction from Chase should produce a visible memory write before the turn ends.

Addendum: grounding gradient generalized same day — "the goal behind the words" → "the intent behind a message, the signal within a result." Inputs aren't always words (interrupts, command results, exit codes, file contents); "observation" is the loop-primitive term, and the two examples span both observation sources (other selves, world).
